Các bước run dự án 

1/ chạy ở Project "firebase emulators:start --import=./emulator-data --export-on-exit" 
nhớ đợi chạy xong , khoảng 20s, hiện ra 2 link auth và firestore là ok (chạy firebase này nhớ để cái terminal nhe , JDK 21 trở lên nhe)

2/ chạy firebase xong mới chạy be nhé 

3/ chạy fe như bth 

Note: Nhớ chạy cái 1/ xong hả chạy cái 2/ 3/ 